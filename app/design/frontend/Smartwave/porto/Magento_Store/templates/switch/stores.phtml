<?php
/**
 * Copyright © 2015 Magento. All rights reserved.
 * See COPYING.txt for license details.
 */

// phpcs:disable

?>
<?php
/**
 * Store switcher template
 *
 * @see \Magento\Store\Block\Store\Switcher
 */
?>
<?php if (count($block->getGroups())>1): ?>
<div class="switcher store switcher-store" id="switcher-store">
    <label for="select-store"><?= $block->escapeHtml(__('Select Store')) ?></label>
    <select id="select-store" title="<?= $block->escapeHtmlAttr(__('Your Store')) ?>" onchange="window.location.href=this.value" class="flags">
    <?php foreach ($block->getGroups() as $_group): ?>
        <?php $_selected = ($_group->getId() == $block->getCurrentGroupId()) ? ' selected="selected"' : '' ?>
        <option value="<?= $block->escapeUrl($_group->getDefaultStore()->getCurrentUrl()) ?>" <?php /* @escapeNotVerified */ echo $_selected ?>><?php echo $block->escapeHtml($_group->getName()) ?></option>
    <?php endforeach; ?>
    </select>
</div>
<?php endif; ?>
